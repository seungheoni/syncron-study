spring:
  application:
    name: syncron-study

  datasource:
    url: jdbc:mysql://localhost:3306/stock_example
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true